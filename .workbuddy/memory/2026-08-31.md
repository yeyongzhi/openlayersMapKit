# 2026-08-31 工作日志

## 代码核查（用户问"代码是否都 ok"）
四道门禁全跑一遍，结论：当前代码健康，无回归。
- typecheck：`tsc --noEmit` 0 error ✅
- eslint：0 error / 7 存量 warning（DragBox·Draw·Modify·Select 的 `OlEvent`、ImageLayer `DEFAULT_IMAGE_STATIC_SOURCE_PARAMS`、TileLayer `OlSource`、`interaction-event-bridge.test.ts` 的 `InteractionExtent`）——与基线一致 ✅
- vitest：全量 27 文件 / 168 用例 passed；报告 "Errors 7 errors" = 正好 7 个 `@vitest-environment happy-dom` 文件因本地 `.pnpm` 缺 `happy-dom` 包无法起 worker（已知沙箱缺口，非本项目问题，靠 CI 验证）✅
- build：`vite build` 603 模块转译、UMD 1.77MB / ES 1.66MB、声明文件生成成功（临时 `emptyOutDir:false` 已还原）✅
- `audit:api`：122 个公开导出 `tests:>=1`，未被测试引用的公开类 = 0 ✅

## Layer/Source 测试覆盖缺口分析
所有 Layer/Source 公开类均有测试引用（无 tests:0）。但以下 8 个类**只有跨类 wrapper 保留断言、无专属行为测试**（仅在 `tests/layer/layer-source-wrapper.test.ts` 被引用，构造参数可见该文件）：
- **Layer（5）**：`WMSLayer`(3)、`WMTSLayer`(3)、`XYZLayer`(3)、`GaodeLayer`(4)、`ImageLayer`(4)
- **Source（3）**：`WMTSSource`(3)、`ImageSource`(4)、`ImageStaticSource`(4)

其中计数最低（=3）、最该补的是 `WMSLayer`/`WMTSLayer`/`XYZLayer`/`WMTSSource` 这 4 个（WMTSSource 甚至无任何独立测试，只经 WMTSLayer 内部构造）。

其余 Layer/Source 类已有专属或较厚覆盖：`VectorLayer`(37)、`TileLayer`(8)、`LayerGroup`(8)、`TdtLayer`(6)、`VectorSource`(16)、`XYZSource`(12)、`TileWMSSource`(7)、`UTFGridSource`(8)、`TileSource`(10)、以及上一轮补的 `DataTileSource`(4)/`ImageTileSource`(6)/`OGCVectorTileSource`(5)/`TileDebugSource`(4)/`VectorTileSource`(5)/`Source`(5)/legacy `TileImageSource`(6)/`UrlTileSource`(6)。

> 待用户决定：是否要为新识别的 8 个类补专属行为测试（建议至少补最薄的 4 个）。

## 用户本意纠正：Layer/Source「增补新类」缺口分析（非补测试）
用户原话"Layer和Source中是否有还可以补充的类 比如 xxxLayer xxxSource" = 给 SDK **新增** OpenLayers 尚未包装的 Layer/Source 类（之前误读成给已有类补测试）。

omap 已包装（含 OL 改名）：Layer 侧 TileLayer/ImageLayer/VectorLayer/LayerGroup/XYZLayer/WMSLayer/WMTSLayer/GaodeLayer/TdtLayer；Source 侧 Source(base)/ImageSource/ImageStaticSource/VectorSource/TileSource(base)/XYZSource/TileWMSSource/WMTSSource/DataTileSource/ImageTileSource(legacy)/TileImageSource(legacy)/UrlTileSource(legacy)/TileDebugSource/UTFGridSource/VectorTileSource/OGCVectorTileSource。

**对照 OL 全量，omap 缺失的常用类（缺口）：**
- Layer：`VectorTileLayer`(关键！现有 VectorTileSource 无对应渲染图层)、`HeatmapLayer`、`VectorImageLayer`、`Graticule`、`WebGL*(Points/Tile/Vector/VectorTile)`(另类 API，暂边缘)
- Source：`OSMSource`(极简，=XYZ+OSM url/attribution)、`BingMapsSource`(需 key+quadkey)、`ClusterSource`(矢量聚合，高价值)、`TileJSONSource`、`TileArcGISRestSource`(企业/国内常用)、`ImageWMS`(ImageLayer 目前只 static/loader 两分支，无 WMS 单图)、`OGCMapTileSource`、`GeoTIFFSource`(高级)、`GoogleSource`(需 key)、`RasterSource`、`IIIFSource`、`StadiaMapsSource`、`CartoDBSource`、`ZoomifySource`、`SentinelHubSource`、`ImageArcGISRest/ImageCanvas/ImageMapGuide`

建议优先批次 P1：`VectorTileLayer` + `OSMSource` + `ClusterSource` + `TileJSONSource` + `BingMapsSource`（高价值、低/中工作量）。P2：`HeatmapLayer`/`TileArcGISRestSource`/`VectorImageLayer`/ImageWMS。P3：其余专业化类。

> 待用户确认：是否按 P1 实现，或先只做最关键的 VectorTileLayer+OSMSource。
