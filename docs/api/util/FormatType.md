# FormatType

> 稳定性：`stable-beta` — 常量枚举（只读对象）

数据格式常量，用于 [`Format`](./Format.md) 的构造。

## 引入

```ts
import { FormatType } from 'openlayers-map-kit'
```

源码：`src/module/util/Format/type.ts`

## 成员

| 常量                 | 值          | 说明 |
| -------------------- | ----------- | ---- |
| `FormatType.GeoJSON` | `'GeoJSON'` | —    |
| `FormatType.WKT`     | `'WKT'`     | —    |
| `FormatType.KML`     | `'KML'`     | —    |

## 说明与注意点

- 当前支持 `GeoJSON`、`WKT`、`KML` 三种格式。

<!-- 本页由 scripts/gen-api-docs.mjs 从源码签名自动生成，请勿手工编辑签名表 -->
