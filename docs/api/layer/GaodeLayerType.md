# GaodeLayerType

> 稳定性：`stable-beta` — 常量枚举（只读对象）

高德底图类型常量，用于 [`GaodeLayer`](./GaodeLayer.md)。

## 引入

```ts
import { GaodeLayerType } from 'omap'
```

源码：`src/module/layer/GaodeLayer/type.ts`

## 成员

| 常量                  | 值       | 说明 |
| --------------------- | -------- | ---- |
| `GaodeLayerType.Vec`  | `'vec'`  | —    |
| `GaodeLayerType.Img`  | `'img'`  | —    |
| `GaodeLayerType.Road` | `'road'` | —    |

## 说明与注意点

- 当前支持 `Vec`（矢量）、`Img`（影像）、`Road`（路网）。

<!-- 本页由 scripts/gen-api-docs.mjs 从源码签名自动生成，请勿手工编辑签名表 -->
