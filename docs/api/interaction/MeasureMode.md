# MeasureMode

> 稳定性：`stable-beta` — 常量枚举（只读对象）

量算模式常量，用于指定 [`Measure`](./Measure.md) 的量算类型。

## 引入

```ts
import { MeasureMode } from 'omap'
```

源码：`src/module/interaction/Measure/type.ts`

## 成员

| 常量                   | 值           | 说明 |
| ---------------------- | ------------ | ---- |
| `MeasureMode.Distance` | `'Distance'` | —    |
| `MeasureMode.Area`     | `'Area'`     | —    |

## 说明与注意点

- 当前支持 `Distance`（距离）与 `Area`（面积）。

<!-- 本页由 scripts/gen-api-docs.mjs 从源码签名自动生成，请勿手工编辑签名表 -->
