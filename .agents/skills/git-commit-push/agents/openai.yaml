interface:
  display_name: "Git 提交与推送"
  short_description: "自动整理代码改动并生成规范提交信息，完成 Git 提交与分支推送"
  default_prompt: "Use $git-commit-push to summarize the current changes, commit them with the project format, and push the branch."
